#include <gmpxx.h>


// 
// Computes P(n,r) and stores this into MPZ ret
//
// P(n,r) is the total number of r-permutations of a set of 
//  cardinality n.
//
// MPZ ret should already be initialized / allocated
//   by the caller.
//
void perm(mpz_t ret, unsigned long int n, unsigned long int r){

    // TODO

    return;
}


//
// Computes (n choose r) and stores this into MPZ ret
//
// C(n,r) is the total number of r-combinations of a set of 
//  cardinality n.
//
// MPZ ret should already be initialized / allocated
//   by the caller.
//
void comb(mpz_t ret, unsigned long int n, unsigned long int r) {

    // TODO

    return;
}

